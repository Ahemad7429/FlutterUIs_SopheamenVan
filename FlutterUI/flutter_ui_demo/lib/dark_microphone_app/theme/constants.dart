import 'package:flutter/material.dart';

const homeBackground = Color(0xFF2B3038);
const cardBackground = Color(0xFF20242A);
const detailBackground = Color(0xFF20242A);
const detailCardBackground = Color(0xFF2B3038);
const primary = Color(0xFF06CACA);
const white = Color(0xFFFFFFFF);
const textWhite = Color(0xFFFFFFFF);
const textBlack = Color(0xFF000000);

const appTitle = TextStyle(
    fontSize: 35, fontWeight: FontWeight.bold, height: 1.5, fontFamily: 'Lato');
const appSubTitle = TextStyle(
    fontSize: 30, fontWeight: FontWeight.bold, height: 1.5, fontFamily: 'Lato');
const contentWhite =
    TextStyle(fontSize: 15, height: 1.5, color: textWhite, fontFamily: 'Lato');
const contentBlack =
    TextStyle(fontSize: 15, height: 1.5, color: textBlack, fontFamily: 'Lato');
